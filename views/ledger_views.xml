<odoo>
    <data>
        <!-- Tree View -->
        <record id="view_veresiye_defteri_tree" model="ir.ui.view">
            <field name="name">veresiye.defteri.tree</field>
            <field name="model">veresiye.defteri</field>
            <field name="arch" type="xml">
                <tree>
                    <control>
                        <button name="print_receipt" type="object" string="Yazdır"/>
                    </control>
                    <field name="partner_id"/>
                    <field name="phone"/>
                    <field name="total_amount" sum="Toplam"/>
                    <field name="paid_amount" sum="Toplam"/>
                    <field name="remaining_amount" sum="Toplam"/>
                    <field name="last_entry_date"/>
                    <field name="date"/>
                    <field name="last_payment_date"/>
                </tree>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_veresiye_defteri_form" model="ir.ui.view">
            <field name="name">veresiye.defteri.form</field>
            <field name="model">veresiye.defteri</field>
            <field name="arch" type="xml">
                <form>
                    <header>
                        <button name="action_save_and_close" type="object" string="Kaydet" class="btn-primary"/>
                        <button name="print_receipt" type="object" string="Yazdır Fiş" class="btn-secondary"/>
                        <button name="action_open_payment_wizard" type="object" string="Ödeme Yap" class="btn-secondary"/>
                        <button name="action_pay_all" type="object" string="Tümünü Öde" class="btn-secondary"/>
                    </header>
                    <sheet>
                        <group>
                            <field name="partner_id"/>
                            <field name="phone" readonly="1"/>
                            <field name="date"/>
                            <field name="paid_amount"/>
                            <field name="last_payment_date" readonly="1"/>
                            <field name="remaining_amount" readonly="1"/>
                        </group>
                        <notebook>
                            <page string="Satırlar">
                                <field name="line_ids">
                                    <tree editable="bottom">
                                        <field name="product_id"/>
                                        <field name="name"/>
                                        <field name="quantity" sum="Toplam"/>
                                        <field name="price_unit" sum="Toplam" widget="monetary"
                                               options="{'currency_field': 'currency_id'}"/>
                                        <field name="subtotal" sum="Toplam" readonly="1" widget="monetary"
                                               options="{'currency_field': 'currency_id'}"/>
                                        <field name="date"/>
                                        <field name="paid_amount" sum="Toplam" widget="monetary"
                                               options="{'currency_field': 'currency_id'}"/>
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Action and Menus -->
        <record id="action_veresiye_defteri" model="ir.actions.act_window">
            <field name="name">Veresiye Kayıtları</field>
            <field name="res_model">veresiye.defteri</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="view_veresiye_defteri_tree"/>
        </record>

        <menuitem id="menu_veresiye_root" name="Veresiye Defteri" action="action_veresiye_partner"/>
        <menuitem id="menu_veresiye_ledger" parent="menu_veresiye_root" action="action_veresiye_defteri"/>
    </data>
</odoo>
